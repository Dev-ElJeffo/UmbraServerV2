<UserControl x:Class="UmbraManager.Views.Admin.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:views="clr-namespace:UmbraManager.Views"
             xmlns:controls="clr-namespace:UmbraManager.Controls">
<DockPanel Margin="8">
    <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,8">
        <Button Content="Atualizar" Command="{Binding RefreshAccountsCommand}"
                Style="{StaticResource ManagerActionOutlinedButton}" Margin="0,0,8,4"/>
        <TextBox Width="220" Margin="0,0,8,4" Text="{Binding BanReason}"
                 materialDesign:HintAssist.Hint="Motivo do ban"/>
    </WrapPanel>
    <DataGrid Style="{StaticResource ManagerDataGrid}" ItemsSource="{Binding Accounts}" IsReadOnly="True">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
            <DataGridCheckBoxColumn Header="Admin" Binding="{Binding IsAdmin}"/>
            <DataGridCheckBoxColumn Header="Banido" Binding="{Binding Banned}"/>
            <DataGridTextColumn Header="Players" Binding="{Binding PlayerCount}" Width="70"/>
            <DataGridTemplateColumn Header="Ações" Width="160">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Ban" Style="{StaticResource ManagerActionButton}"
                                    Command="{Binding DataContext.BanAccountCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                            <Button Content="Unban" Style="{StaticResource ManagerActionOutlinedButton}"
                                    Command="{Binding DataContext.UnbanAccountCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
    </DataGrid>
</DockPanel>
</UserControl>
